{% include "twig/header.twig" %}
<main>
  <div class="container">
    <div class="row">
      <div class="col-xs-12">
        {% block content %}
          <h1>{{ post.post_title }}</h1>
          {{ post.content }}
        {% endblock %}
      </div>
    </div>
  </div>
</main>
{{ function( 'get_footer' ) }}
